apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: doc-stage
  project: akshay
spec:
  active: true
  config:
    concurrency: 51
    docker:
      limits:
        cpu: 251m
        memory: 1Gi
    environment:
      numWorkers: 11
  type: Docker
  version: r4.0.1
