apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: k8s-agent
  project: defaultproject
spec:
  cluster:
    name: akshay-amd-tb
  config:
    concurrency: 55
    environment:
      numWorkers: 15
    kubernetes:
      limits:
        cpu: 255m
  type: Cluster
  version: r4.1.0
