apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: tf-k8s-agent-version-demo
  project: defaultproject
spec:
  active: true
  cluster:
    name: akshay-amd-tb
  config:
    concurrency: 54
    environment:
      numWorkers: 15
    kubernetes:
      limits:
        cpu: 254m
        memory: 1Gi
  type: Cluster
  version: r4.0.0
