apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: k-agent
  project: akshay-2
spec:
  active: true
  cluster:
    name: agent-test-qc
  config:
    concurrency: 55
    environment:
      numWorkers: 15
    kubernetes:
      limits:
        cpu: 255m
  type: Cluster
  version: r4.1.0
