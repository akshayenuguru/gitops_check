apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: agent-doc
  project: defaultproject
spec:
  config:
    concurrency: 51
    docker:
      limits:
        cpu: 250m
        memory: 1Gi
    environment:
      numWorkers: 11
  type: Docker
  version: r4.0.0
