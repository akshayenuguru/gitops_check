apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: akshay-docker-agent-prod
  project: akshay-2
spec:
  active: true
  config:
    concurrency: 50
    docker:
      limits:
        cpu: 250m
        memory: 2Gi
    environment:
      numWorkers: 10
  type: Docker
  version: r4.0.0
