apiVersion: gitops.k8smgmt.io/v3
kind: Agent
metadata:
  name: doc-agent-1
  project: akshay
spec:
  active: true
  config:
    concurrency: 55
    docker:
      limits:
        cpu: 255m
        memory: 1Gi
    environment:
      numWorkers: 15
  type: Docker
  version: r4.0.1
